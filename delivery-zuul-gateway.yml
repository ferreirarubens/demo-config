server:
  port: 80
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:9091/eureka/
  instance:
    hostname: delivery-zuul-gateway
    home-page-url: http://${eureka.instance.hostname}:${server.port}/
